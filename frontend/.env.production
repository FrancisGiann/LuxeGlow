# ── Production build (vite build reads this automatically) ────
# The app is served same-origin from the XAMPP sub-folder, so API and
# asset calls must carry the sub-path prefix.
VITE_API_BASE=/luxeglow
VITE_LEGACY_APP_URL=/luxeglow
